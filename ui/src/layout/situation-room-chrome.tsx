/**
 * Situation Room chrome — BgFx (grid + scanline overlay). Always rendered;
 * CSS hides it under the classic skin.
 */
export function BgFx() {
  return <div className="afs-bg-fx" aria-hidden="true" />;
}
